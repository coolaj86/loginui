html
  head
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js")
  body
    #js-message
    script
      var loc = JSON.stringify(location, null, '  ')
        ;

      $('#js-message').html('<pre>' + loc + '</pre>');
      window.opener.fbOauth(loc);
